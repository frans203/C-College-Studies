#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    char c;
    printf("Enter one character:");
    scanf("%c", &c);
    printf("The current value of it on decimal value(from ASCII table) is: %d", c);
    return 0;
}