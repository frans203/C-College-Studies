#include <stdio.h>
#include <stdlib.h>

int main() {
    int age = 40; //integer
    double gpa = 3.7; //double to represent decimal numbers.
    char grade = "A"; //char only stores one SINGLE character
    char phrase[] = "STRING"; //how to store an string, a bunch of chars together
    return 0;
}