#include <stdio.h>
#include <stdlib.h>

int main() {
    int money, grade;
    printf("Enter money and grade: \n");
    scanf("%d%d", &money, &grade);

    if(money < 50 && grade > 90){

    }

    if(money < 50 || grade > 90 ){

    }
    if(!(grade > 80)){
        printf("Good Job! \n");
    }else{
        
    }
    return 0;
}