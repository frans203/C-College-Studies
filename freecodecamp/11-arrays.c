#include <stdio.h>
#include <stdlib.h>

int main() {
    // int luckyNumbers[] = {4, 2, 1};
    // luckyNumbers[0] = 2032;
    int luckyNumbers[10]; //potential to hold 10 items
    char phrase[20] = "Array"; //string of characters, array of characters
    luckyNumbers[1] = 80;
    luckyNumbers[0] = 90;
    printf("%d", luckyNumbers[0]);


    return 0;
}